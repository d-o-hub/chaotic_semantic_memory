[package]
name = "csm-persistence"
version = "0.3.7"
edition = "2024"
rust-version.workspace = true
description = "Persistence backends for chaotic_semantic_memory"
license = "MIT"
repository = "https://github.com/d-o-hub/chaotic_semantic_memory"

[dependencies]
csm-core-lib = { path = "../csm-core-lib", version = "0.3.7" }
csm-memory = { path = "../csm-memory", version = "0.3.7" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true }
uuid = { workspace = true, optional = true, features = ["js"] }

# libSQL backend - gated to non-WASM targets only (mio incompatible with wasm32)
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
libsql = { version = "0.9.30", default-features = false, features = ["sync", "hrana", "remote"], optional = true }
# `fs` is used by persistence_ops (tokio::fs); declared explicitly instead of
# relying on libsql's transitive tokio features. Non-wasm only: tokio fs does
# not compile on wasm32.
tokio = { workspace = true, features = ["fs"] }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
tempfile = { workspace = true }

[features]
default = ["persistence"]
persistence = ["dep:libsql"]
libsql = ["persistence"]
wasm = ["uuid"]

[lib]
crate-type = ["lib"]

[lints]
workspace = true