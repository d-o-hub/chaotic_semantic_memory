<<<<<<< SEARCH
        // SAFETY: We transmute the Hnsw instance to 'static lifetime because we
        // store the 'loader' that owns the underlying data in 'self._owner',
        // ensuring it outlives 'self.hnsw'.
        let static_hnsw: Hnsw<'static, HVec10240, HammingDist> =
            // SAFETY: Manual audit required. Restoration of CI gate.
            unsafe { std::mem::transmute(hnsw) };
=======
        // SAFETY: We transmute the Hnsw instance to 'static lifetime because we
        // store the 'loader' that owns the underlying data in 'self._owner',
        // ensuring it outlives 'self.hnsw'.
        let static_hnsw: Hnsw<'static, HVec10240, HammingDist> =
            unsafe { std::mem::transmute(hnsw) };
>>>>>>> REPLACE
