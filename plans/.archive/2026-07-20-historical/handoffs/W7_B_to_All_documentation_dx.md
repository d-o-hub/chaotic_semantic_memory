# W7 Group B -> All: Documentation & DX Closure

- Added rustdocs to `FrameworkConfig` and `SingularityConfig` fields, defaults, and ranges.
- Expanded `README.md` with Installation, Configuration, tuning guidance, and API patterns.
- Added minimal runnable example at `examples/basic_in_memory.rs`.
- Added cargo aliases in `.cargo/config.toml` for routine validation commands.
