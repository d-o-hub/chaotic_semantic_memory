# PR Roast & Triage — 2026-09-08

> **Session Status (2026-09-08):**
> - **Open PRs at start:** 7 (#679, #677, #676, #675, #674, #673, #660) — all MERGEABLE, all CI truth-verified green (0 fail, 0 pending on every check incl. Codacy/Sonar).
> - **Closed (no impact):** none — every PR carried an independent, non-overlapping change (no duplicate clusters; #660 touches exactly 1 file, the suspected `deny.toml` hunk was a stale local `origin/main` artifact).
> - **Merged:** #679 (`main@a73b8a0`).
> - **Pending:** #660 (branch updated, CI re-running), #677/#676/#675/#674/#673 (BEHIND after #679 landed; `@dependabot rebase` requested on all five).

---

## 1. Roast of Open PRs

| PR | Type / Scope | Title | CI | Roast & Recommendation |
|---|---|---|---|---|
| **#679** | `perf(retrieval)` | optimize hash lookups in graph expansion and fact deduplication | ✅ green | **Roast:** (1) Arrived as a **draft** — `gh pr merge` failed with `mergePullRequest: still a draft`; Jules PRs must be marked `gh pr ready` during triage. (2) Two commits with **identical headlines** — history noise, absorbed by squash. (3) Body claims "Eliminates 50% of hash table lookups" without attached Criterion output — accepted only because the transformation is semantically identical (`contains`+`insert` → single `insert`) and the Benchmark CI job guards regressions. **Verdict: merged** (`a73b8a0`). |
| **#660** | `fix(retrieval)` | cap GraphRAG traversal expansions at 1000 nodes | ✅ green (re-running after branch update) | **Roast:** (1) **Stale body** still describing the superseded re-export work that already landed on main — body rewritten via REST API (`gh api -X PATCH`, never `gh pr edit`). (2) Branch carried 4 stale merge commits from repeated main syncs — squash absorbs. (3) `MAX_TRAVERSAL_EXPANSIONS: usize = 1000` is hardcoded; follow-up recommendation: lift it into `GraphRagConfig` (default 1000) so dense-graph operators can tune it. (4) Cap semantics are sound: `Entry::Occupied` score updates bypass the cap, so already-seen nodes still improve; only new expansions are bounded. **Verdict: keeper — merge after branch update CI is green.** |
| **#677** | `chore(deps)` | bump uuid 1.24.0 → 1.26.0 | ✅ green | Clean dependabot minor bump (`Cargo.lock` only). **Roast:** title's double-scope `chore(deps)(deps)` only survives commitlint via the `updated-dependencies` ignore rule — cosmetic, not blocking. **Verdict: merge after `@dependabot rebase`.** |
| **#676** | `chore(deps)` | bump hyper 1.11.0 → 1.11.1 | ✅ green | Clean patch bump (`Cargo.lock` only). Same double-scope cosmetic note. **Verdict: merge after rebase.** |
| **#675** | `ci(deps)` | bump taiki-e/install-action 2.87.2 → 2.87.5 | ✅ green | Workflow-SHA bump in `pre-release-gate.yml` only. **Verdict: merge after rebase.** |
| **#674** | `ci(deps)` | bump softprops/action-gh-release 3.0.2 → 3.0.3 | ✅ green | SHA bumps in `dist.yml` + `release.yml`. **Verdict: merge after rebase.** |
| **#673** | `ci(deps)` | bump actions/deploy-pages 5.0.0 → 5.0.1 | ✅ green | SHA bump in `pages.yml` only. **Verdict: merge after rebase.** |

---

## 2. Triage Decisions

- **Duplicate detection:** no clusters — the two Jules PRs touch disjoint files (`singularity_retrieval.rs`+`bridge_retrieval.rs` vs `graph_rag.rs`); dependabot PRs each touch disjoint files.
- **Branch protection gotcha:** all PRs must be up to date with `main` before merge. After #679 landed, #660 became `BEHIND`-style-blocked and was fixed via `gh api -X PUT repos/.../pulls/660/update-branch` (the installed `gh` has **no** `gh pr update-branch` subcommand). Dependabot PRs were queued with `@dependabot rebase` comments so they rebase **once** onto final main.
- **No `--auto` merges** — manual sequential merges only (AGENTS.md: never `gh pr merge --auto` on multiple PRs).

---

## 3. Merge Order (executed)

1. **#679** perf(retrieval) hash-lookup micro-opt — ✅ MERGED `a73b8a0`
2. **#660** fix(retrieval) GraphRAG expansion cap — after update-branch CI green
3. **#677** uuid minor bump — after rebase CI green
4. **#676** hyper patch bump
5. **#675** taiki-e/install-action bump
6. **#674** softprops/action-gh-release bump
7. **#673** actions/deploy-pages bump

Each step: `gh pr checks <n>` all green → `gh pr merge <n> --squash --delete-branch` → verify next PR's `mergeStateStatus`, re-request rebase if `BEHIND`.

---

## 4. Follow-ups (roast recommendations encoded)

- Make `MAX_TRAVERSAL_EXPANSIONS` configurable via `GraphRagConfig` (default 1000) — queued as a recommendation, not merged blind in a docs pass.
- Triage workflow additions encoded in `progress/LEARNINGS.md`: draft-Jules-PR gate, `gh api update-branch` fallback, single-pass `@dependabot rebase` batching.
