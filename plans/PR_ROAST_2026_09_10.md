# PR Roast 2026-09-10 — #683 (+ main health)

## Verdict table

| PR | Title (after) | Verdict | Reason |
|----|---------------|---------|--------|
| #683 | refactor(memory): simplify LSH hash bit math and candidate dedup types | FIX + MERGE (as hygiene, not perf) | Correct, zero behavior change; original `perf(csm-memory)` scope illegal + perf claims unbenchable. Reworked: single atomic commit, honest comments, regression test kept |
| #677 | chore(deps)(deps): uuid 1.24 → 1.26 | KEEP (trivial green, merge first) | No overlap with #683; not in scope of this session |

No duplicates of #683. Only 2 open PRs; #677 touches Cargo.lock only.

## #683 roast (GOAP swarm: DuplicateHunter, PerfEvidenceScout, CorrectnessRoaster, CiTruthChecker)

- **Impact**: NONE measurable. `/8` on `usize` strength-reduces to `>>3` in LLVM (`udiv` → `lshr`; rustc `/` → LLVM `udiv`); `%8` → `&7`. `HashSet` is documented as "a HashMap where the value is ()" (std docs) — same hashing cost, `()` zero-sized. Zero LSH criterion targets exist (9 bench files, none match lsh/AnnIndex) so the `perf` title was unfalsifiable. Per pr-roast-triage rubric + AGENTS.md#9: unmergeable as `perf`.
- **Correctness**: clean at head (empty slices, bit_pos ≥ 5120 valid via 1280-byte HVec10240, top_k=0 guards, both rayon/wasm cfg branches, `to_string()` preserves `Vec<(String,f32)>` contract). No bug.
- **CI truth**: 30+ checks green; single REAL fail was commitlint scope `csm-memory` (not in scope-enum; `memory` is). NOT an infra flake.
- **Recommendations applied on branch** (force-pushed `b65c78c`):
  1. Squashed 4 Jules commits → 1 atomic `refactor(memory)` commit (fixes scope + title honesty; drops unrelated merge churn).
  2. PR retitled + body rewritten (no perf claim).
  3. False "eliminate Entry allocations / String pointer indirection" comments corrected at `lsh.rs` search + search_filtered.
  4. Kept: dedup + top_k=0 regression test, `.get()` single-access, RetrievalConfig struct literals (matches coding-standards direct-construction rule).

## Main health (pre-existing, unrelated to #683)

- Main HEAD `f6117e7` run 34458833944 = FAILURE from commitlint `subject-case` on squash commit `docs: PR roast 2026-09-08 plan…` ("PR" caps). Merged history — unfixable by reword; new pushes validate their own ranges, so a clean squash merge heals HEAD.
- `Unable to authenticate to FlakeHub` annotations = known infra flake (re-run, don't fix code).

## Merge order

1. #677 (dependabot, trivial) — owner merges.
2. #683 after all checks green — `gh pr merge 683 --squash --delete-branch`.
