# PR Roast 2026-09-11 — Stale-Base Squash Revert Repair

## Headline finding

The main history acquired by squash-merging the Jules PR #683 lineage
(`a3eeaab` → `9140aa4` → `fdcd069` → `ccbb7e8`) is **not additive**. `9140aa4`
was forked before a3eeaab/1d1b187/fdadc72 and its squashed tree reverted every
file it did not intend to touch:

| Path | Intended (a3eeaab / 1d1b187 / f6117e7) | State before repair |
|------|----------------------------------------|---------------------|
| `.github/workflows/dist.yml` | `softprops/action-gh-release@efb3536… # v3.0.3` (#674) | `@3d0d988… # v3.0.2` |
| `.github/workflows/release.yml` | same v3.0.3 pin | `@3d0d988… # v3.0.2` |
| `Cargo.lock` | `uuid 1.26.0` + `getrandom 0.3.4` for tempfile (#677) | `uuid 1.24.0`, `getrandom 0.4.3` |
| `crates/csm-memory/src/index/lsh.rs` | `>> 3`/`& 7` + `bytes.get` + `with_capacity(32)`, honest comments, `search_filtered` test | `/ 8`, `% 8`, `HashSet::new()`, false "eliminate Entry allocations" comments |
| `crates/csm-memory/src/singularity_retrieval_tests.rs` | `RetrievalConfig { .., ..Default::default() }` literals | field-by-field mutation |
| `progress/LEARNINGS.md` | 2026-09-08 compaction with PR #679 / CI-queue lessons | pre-compaction rewrite |
| `progress/PROGRESS.md` | 2026-09-08 wave entry | 14 lines deleted |
| `plans/PR_ROAST_2026_09_08.md` | present | deleted |

## PR verdicts

| PR | Verdict | Reason |
|----|---------|--------|
| #686 | MERGED (`ccbb7e8`) | Restores the LSH production-code hunks lost to `9140aa4`; green 30/30. Retitled `perf(memory)` → `refactor(memory)` before merge (LLVM already strength-reduces `/8`→`>>3`; only `with_capacity(32)` is a real, unmeasured allocation change). GitHub kept the branch commit subject as the squash subject. |
| #684 | MERGE after branch sync | Real CWE-770 fix: `BridgeRetrieval::query` (public via `lib.rs`) accepted unbounded `top_k`; `Framework` callers pre-validate but direct API callers did not. Matches `csm-retrieval/src/bm25.rs:226` clamp precedent. CI green, `mergeStateStatus: CLEAN`. |
| #685 | HOLD for review | Feature-gated `experimental-ils3d` (Sun et al. 2025) hyperchaotic map; `--all-features` CI compiles and tests it. Open question: doc comment says "Based on 3D-CCC principles" while the module claims the paper's ILS map — verify equation fidelity or reword before merge. |
| #683 lineage | n/a | Already merged; repaired via this PR. |

## Repair PR content

1. `ci`: restore v3.0.3 action pin in `dist.yml` + `release.yml`.
2. `deps`: restore `Cargo.lock` from `1d1b187` (verified with `cargo metadata --locked`).
3. `memory`: restore corrected LSH comments and `RetrievalConfig` struct-literal
   test construction; keep the `search_filtered` dedup test added by `9140aa4`.
4. `docs`: restore `progress/LEARNINGS.md`, `progress/PROGRESS.md`,
   `plans/PR_ROAST_2026_09_08.md`; add the 2026-09-10 and 2026-09-11 roast records.

## Prevention

- Pre-merge check: `git diff <fork-point> <squash-commit> --stat` must list only
  paths the PR intends; a file the subject never mentions is a revert candidate.
- Encoded in `progress/LEARNINGS.md` → "Stale-base squash reverts (2026-09-11)".

## Merge order

1. Repair PR (this branch) — restores main truth.
2. #684 after `update-branch` + green CI.
3. #685 after naming-fidelity review.

## Main health

`ccbb7e8` (CI, Release, CodeQL, benchmark-ci) green. Queued GOAP actions unchanged:
`deduplicate_persistence_owner_bodies` (P1), `add_ann_and_persistence_scale_benchmarks`,
`replace_formula_only_memory_claim` (P2).
