# PR Roast — 2026-09-18

Two open PRs at the start of the session, both Jules (`google-labs-jules[bot]`)
branches. Baseline: `./scripts/pr-triage.sh` — both `MERGEABLE`, both `BEHIND`
main (`f12df7a`, the evidence wave of #732–#736), zero conflicts.

## Verdict table

| PR | Title | Diff | CI truth | Verdict |
|---|---|---|---|---|
| #735 | `fix(framework): validate graph rag config parameters` | +118/−6, 3 files (`framework_graph_rag.rs`, `framework_validation.rs`, `LEARNINGS.md`) | 28 pass / 5 skipped, 0 fail; Codacy 0 issues; Sonar gate passed | **Keeper** — merge after `update-branch`. Two non-blocking nits. |
| #737 | `perf(memory): optimize LSH hash computation and candidate scoring` | **0 files** (`gh pr view --json files` → `[]`, `git show f021393c --stat` empty) | 35 pass — vacuous, measured against a tree identical to main | **Closed as no-op** |

## #735 — keeper detail

Real gap: `probe_with_graph` validated only `anchor_top_k`/`final_top_k`
before this change, so `max_hops`, `min_assoc_strength`, `similarity_weight`
and `graph_weight` reached scoring/traversal unchecked (same class as the
2026-08-07/17 public-input-bounds cluster in `progress/LEARNINGS.md`).

- `MAX_TRAVERSAL_DEPTH = 32` matches `crates/csm-memory/src/graph_traversal.rs:20`
  — the framework fails fast with `MemoryError::InvalidInput { field }` instead of
  inventing a new bound or deferring to the owner crate's error.
- No bypass: CLI `run_probe_graph` and wasm `probe_text_with_graph` both funnel
  through `probe_with_graph` (`src/framework_graph_rag.rs:51`).
- `anchor_top_k == 0` / `final_top_k == 0` keep skipping `validate_top_k`,
  matching previous behaviour.
- Tests: integration case on `max_hops` + unit case covering all six fields.

Nits (non-blocking): (1) the CLI maps graph-rag failures to
`CliError::Persistence`, so `--hops 100` is reported under a persistence label
(`src/cli/commands/probe_graph.rs:35`); (2) independent weight validation allows
`similarity_weight + graph_weight == 2.0` — worth a comment stating the weights
need not sum to 1.0.

## #737 — closed as no-op

The branch carries no changes at all: one empty commit, and everything the PR
description claims is absent from the diff. The fully green check matrix is not
evidence of anything, because it ran against tree == main.

Adjudicated the two ideas anyway, for a resubmission:

Allocator counting (wrapping `GlobalAlloc`, 126 candidates all valid — the
average candidate count measured at 50 k in
`plans/evidence/scale_2026_09_17/ann_scale.json`):

```
filter_map(..).collect()       len=126  allocs=132  bytes=8584
with_capacity + push           len=126  allocs=127  bytes=4552
map(..).collect() [TrustedLen] len=126  allocs=1    bytes=2016
```

- Pre-sizing `score_specific_candidates` saves ~5 of 132 allocations; the other
  126 are the per-item `id.clone()`
  (`crates/csm-memory/src/singularity_retrieval.rs:285`), inherent to the
  `Vec<(String, f32)>` return type.
- `filter_map` is not `TrustedLen`, so the growth is real here — unlike the
  "`collect()` always reallocates" claim refuted in #706 (where the iterator was
  a slice `map`). Both belong in the same LEARNINGS bullet.
- The LSH idea (`bytes.get(byte_idx)` at
  `crates/csm-memory/src/index/lsh.rs:73-86` → length assert + direct indexing)
  removes ~80 predictable branches per query at 5 tables × 16 bits, against 126
  clones + 126 Hamming distances. Needs a bench: `benches/` has no LSH target,
  and the baseline to beat is LSH p50 813 µs / 28MB-class build cost per
  `plans/evidence/scale_2026_09_17/ann_scale.json`.

## Merge order (manual, per the skill — not executed here)

1. **#735** — `gh api -X PUT repos/d-o-hub/chaotic_semantic_memory/pulls/735/update-branch`,
   wait for CI, then `gh pr merge 735 --squash --delete-branch`.
2. #737 — closed; nothing to merge.

```bash
git fetch origin main
gh pr checks 735                      # expect all green after the branch update
git diff --stat origin/main...fix/security-graph-rag-bounds-7732578195745535547
gh pr merge 735 --squash --delete-branch
```
