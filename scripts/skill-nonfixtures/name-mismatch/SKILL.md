---
name: wrong-name
description: "Test fixture whose frontmatter name does not match its directory."
---

# Name Mismatch Fixture

The directory is `name-mismatch` but the frontmatter says `wrong-name`. The
validator must reject the mismatch.
